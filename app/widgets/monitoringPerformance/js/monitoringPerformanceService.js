angular.module('widgets').service('intellimonitoringPerformanceService', [
    'APIService', '$q',
    function(APIService, $q) {
        'use strict';
        
        
        return { 
            
        };
    }
]);