angular.module('widgets').service('intelliserviceModelingService', [
    'APIService', '$q',
    function(APIService, $q) {
        'use strict';

        

        return {
            // "addCred" : addCred,
            // "getCredDetlist": getCredDetlist,
            // "updateCreddet" : updateCreddet,
            // "deletcreddet" : deletcreddet
        };
    }
]);